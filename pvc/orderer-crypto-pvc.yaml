kind: PersistentVolumeClaim
apiVersion: v1
metadata:
  name: orderer-pv-volumeclaim
spec:

  accessModes:
    - ReadWriteMany
  selector:
    matchLabels:
      type: orderer-pv-volume
      purpose: dev
  resources:
    requests:
      storage: 3Gi
